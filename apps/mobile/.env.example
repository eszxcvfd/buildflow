# Copy to `apps/mobile/.env.local`. For Expo Go on a physical device, replace
# this example with the development machine's LAN IPv4; both devices must use
# the same network. Android Emulator may use `http://10.0.2.2:3000/api/v1`.
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api/v1
