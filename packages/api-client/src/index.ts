/**
 * Public entry point for the generated BuildFlow API client.
 *
 * The `paths` and `components` types come from `src/generated/api.d.ts`
 * (produced by `openapi-typescript`). Consumers (apps/web, apps/mobile,
 * tests) should import from this file rather than the generated path.
 */
export type { paths, components, operations } from './generated/api';
