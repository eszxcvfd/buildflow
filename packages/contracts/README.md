# @buildflow/contracts

Cross-client **technical** contract types only.

Do not place business entities, DTOs or domain models here. Generated API
types live in [`@buildflow/api-client`](../api-client). Business contracts
are owned by the producing module in `apps/api`.
