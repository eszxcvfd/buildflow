{
  "name": "@buildflow/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared cross-client technical contracts (placeholder; no business entities).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@buildflow/contracts has no tests yet\" && exit 0",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@buildflow/eslint-config": "workspace:*",
    "@buildflow/tsconfig": "workspace:*",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0"
  }
}
